message.welcome: "Reservation Service Welcome!!"

spring.cloud.stream.bindings.input.destination: reservations

management:
  security:
    enabled: false
  context-path: /admin
logging:
  level:
    root: WARN